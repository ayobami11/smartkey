import { KeyIdView } from './_components/key-id-view';

export const metadata = {
  title: 'Key Details | CSO',
  description: 'Manage collectors for Administration key.',
};

export default function KeyIdPage() {
  return <KeyIdView />;
}
