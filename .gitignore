# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage
/playwright-report
/test-results
# Playwright storageState files (tests/e2e/auth.setup.ts) — live session cookies
/playwright/.auth

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
Thumbs.db
desktop.ini
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*
!.env.local.example

# supabase local dev artifacts
supabase/.branches
supabase/.temp
supabase/volumes

# claude code local settings (personal overrides, not for team sharing)
.claude/settings.local.json
CLAUDE.local.md

# vercel
.vercel

# generated review artifacts (not project documentation)
docs/SmartKey_Examiner_Review.pdf

# internal/operational notes — not for shared git history. E2E_OTP_SETUP
# documents the test-mailbox +tag naming scheme (operational infra detail);
# SIGNATURE_CALIBRATION_TEST_RUN names real colleagues in a forgery-testing
# context. Neither belongs in tracked source.
docs/E2E_OTP_SETUP.md
docs/SIGNATURE_CALIBRATION_TEST_RUN.md
docs/SESSION_CREDENTIALS.md

# typescript
*.tsbuildinfo
next-env.d.ts
.claude/worktrees/

# real signature images for calibration — never commit these (personal, sensitive)
tests/signature-calibration-samples/**/*.png
tests/signature-calibration-samples/**/*.jpg
tests/signature-calibration-samples/**/*.jpeg
tests/signature-calibration-samples/**/*.webp

Project Guide Second Semester new.docx
SmartKey Project Report.md

# generated backend implementation report and assets
backend/implementation/
